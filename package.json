{
  "name": "google-sheets-reader",
  "version": "0.1.0",
  "description": "Amplience Workforce extension — Google Sheets read + write (tabs, rows, columns, lookups, append, update)",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "validate": "node validate.mjs",
    "check": "node --test && node validate.mjs"
  }
}
